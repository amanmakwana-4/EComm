// Minimal runtime replacements for generated TypeScript DB types.
// The full TypeScript types are removed; this file provides runtime
// constants that some code may rely on.
export const Constants = {
  public: {
    Enums: {
      app_role: ["admin", "customer"],
    },
  },
};

export default {};
